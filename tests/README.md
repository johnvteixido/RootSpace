# Tests

RootSpace unit and integration tests are strictly co-located with their respective source files in the `src/` and `rust-v2/src/` directories (e.g., `src/agent-api.test.js`). 

This directory is reserved for future end-to-end (E2E) cross-language network emulation tests.
