services:
  daemon:
    build: .
    ports:
      - "5000:5000"
      - "3000:3000"
    environment:
      - AGENT_API_KEY=CHANGE_ME_GENERATE_A_SECURE_KEY
      - PORT=5000
      - AGENT_API_PORT=3000
    networks:
      - rootspace-net

  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    environment:
      - VITE_DAEMON_URL=ws://localhost:3000
    depends_on:
      - daemon
    networks:
      - rootspace-net

networks:
  rootspace-net:
    driver: bridge
